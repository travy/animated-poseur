/*
 * To change this template, choose Tools | Templates
 * and open the template in the editor.
 */
package poseur.io;

import java.io.File;

/**
 * The class <code>SpriteTypeIO</code> knows everything that there is to know
 * about Sprite Type objects so that there information can be saved to HDD.
 * <code>FileManager</code> objects will depend upon this class when saving the
 * state of a project to disk.  Along with saving data, this class is also
 * useful for opening previously saved files from HDD so that the user can
 * continue on a project that they closed the day before.
 * 
 * @author      Travis Anthony Torres
 * @version     1.0     November 2012       Initial Release
 */
public class SpriteTypeIO {
    
    /**
     * 
     */
    public SpriteTypeIO() {
        //  TODO
    }
    
    /**
     * Will store all of the information tied to the current Sprite Type object
     * to HDD so that the user can retrieve the information at a later time.
     * 
     * @param   file
     *          The file that the routine will store data within.
     */
    public void save( File file ) {
        //  TODO
    }
    
}
